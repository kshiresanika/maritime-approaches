# STATUS — EDTH Hamburg / Topic 02

One line per session. Newest at the bottom. Never delete a line; correct with a new one.

Format: `YYYY-MM-DD HH:MM | lane | what changed | what is now blocked`

## Demo target ladder — REVISED 2026-08-28, INDOOR ONLY
**No water access. No Elbe, no harbour, no line of sight to a vessel. Venue is indoors.**
Full plan in `04_demo/DEMO_INDOOR.md`. That file governs; this is the summary.
- **D0 (guaranteed)** — replay harness. EO contacts forward-projected from the real DMA
  golden window through a declared pose, with deliberately injected faults and a
  known-ground-truth false-positive control. No camera required. Build this first.
- **D1 (live proof)** — tabletop scale range. Toy boats / printed silhouettes, surveyed
  pose, 1 cm = 20 m. Real YOLO, real pinhole geometry, spoof induced by physically
  swapping a silhouette. Kill risk: COCO `boat` may not fire on paper — test early.
- **D2 (fallback)** — camera pointed at a screen playing a harbour clip. Proves detection
  liveness only. Must run with `uncalibrated_benchmark_pose()` (yaw sigma 180) so no
  bearing or position claim can reach significance.
- **T2 (live Elbe + live AIS) — CANCELLED, not deferred.** Do not attempt.

## Criteria ledger
| # | Criterion | Owner module | Status |
|---|---|---|---|
| 1 | EO frame + AIS in -> MATCH / dark / spoof verdict, with bearing + rough range | eo_detector, ais_ingest, association, verdict | ☑ written, UNRUN |
| 2 | Prioritization — ranked, not a flat alert list | prioritizer | ☑ written, UNRUN |
| 3 | AIS spoofing (claimed vs. observed), not only AIS-off | consistency | ☑ written, UNRUN |
| 4 | Evidence-grade output + honest limits / defer-to-human | evidence, app | ☑ written, UNRUN |

## Ledger
2026-08-27 08:05 | setup | Repo audited before hacking starts. Read CLAUDE.md, 00_brief/*, 01_research/ais_spoofing_methods.md, 02_data/DATASETS.md. Created STATUS.md, FILE_OWNERSHIP.md, 99_scratch/requests.md. | BLOCKED: no macOS-side verification yet — the agent shell is a sandboxed Linux VM with no network, so MPS FPS, camera, Featherless and the AIS download endpoint are all UNVERIFIED. 03_src/LIBRARIES.md and 03_src/contracts.py are referenced by CLAUDE.md but do not exist. Venue line-of-sight to water is unknown and gates T2.
2026-08-27 13:36 | setup | First macOS-side verification run (verify_env.sh, executed by Amol). MEASURED: Apple M4 / macOS 26.5.2 / python.org Python 3.12.10. torch 2.13.0 MPS built+available. yolov8n @640x640 device=mps = 45.0 FPS (22.2 ms/frame, 30 frames after 5 warmup, synthetic frame). Camera PASS at 1920x1080. Present: pandas 3.0.5, ultralytics 8.4.130, torch 2.13.0, cv2 5.0.0. | BLOCKED: pyais, geopandas, shapely, movingpandas, pydantic all missing. DMA bulk endpoint returns 302 and the index could not be enumerated — PRIMARY data source unconfirmed, T1 gated on it. Featherless /models returned 401 — no verified LLM path, criterion 3 rationale gated on it. RISK: pandas 3.0.5 may conflict with the geopandas/movingpandas stack; use a project venv, do not install into the framework Python.
2026-08-27 14:40 | setup | Featherless verified end to end. Key created under project-scoped name EDTH_FEATHERLESS_KEY in ~/.config/edth-hamburg/edth-hamburg-2026.env (never in the repo). GET /models 200, 21836 models. Chat 200 and VISION 200 both on Qwen/Qwen3-VL-30B-A3B-Instruct — the vision call correctly described a synthetic test image, so the image transport path is proven, not assumed. MEASURED from /v1/models: context_length 131072, max_completion_tokens 32768, image_input true, tool_use true, concurrency_cost 2, $0.15/M in $0.50/M out, available_on_current_plan true. Fallback Qwen/Qwen2.5-VL-72B-Instruct (32768 ctx, concurrency_cost 4, ~5x price). Llama-3.3-70B-Instruct REJECTED as primary: is_gated true, no image_input. Corrected constraint 12 — see requests.md. | BLOCKED: DMA 302 still undiagnosed, T1 data source unconfirmed. pyais/geopandas/shapely/movingpandas/pydantic still not installed. Venue line-of-sight still unknown.
2026-08-27 15:05 | setup | CORRECTION to the 14:40 line: the DMA 302 was NOT the endpoint moving. curl -IL showed Location: https://login.wifionice.de/bounce_er/web.ais.dk/aisdata/ — Deutsche Bahn WIFIonICE captive portal intercepting plain HTTP (port 80) and returning its own 500. HTTPS passes untouched, which is why Featherless/pip/GitHub all worked. Lesson: any network result measured behind a captive portal is provisional until re-run on a clean link. DISCOVERED: aisdata.ais.dk is a static s3-bucket-listing page (S3BL_IGNORE_PATH, BUCKET_NAME) rendering an S3 bucket client-side — so the file list is obtainable from the S3 ListObjects XML API without JavaScript, once the bucket name is read from the page. https://aisdata.ais.dk/ returned empty on this link; retry from A&O hotel wifi or phone hotspot. | BLOCKED: DMA file list, sizes and naming scheme still unknown. T1 data source unconfirmed. Libraries install proceeding on train wifi (HTTPS unaffected).
2026-08-27 15:40 | setup | Libraries installed in .venv (--system-site-packages). MEASURED: mps True | pandas 3.0.5 (NO downgrade — the pandas-3 conflict I warned about did not materialise) | geopandas 1.1.4. pyais, shapely, movingpandas, pydantic all import. AIS SOURCE RESOLVED to an S3 bucket: BUCKET_NAME aisdata.ais.dk, region eu-central-1, BUCKET_URL http://aisdata.ais.dk.s3.eu-central-1.amazonaws.com. Root cause of the TLS failures now understood: the bucket name CONTAINS DOTS, so virtual-hosted-style HTTPS breaks against the *.s3.eu-central-1.amazonaws.com wildcard cert. Fix is PATH-STYLE: https://s3.eu-central-1.amazonaws.com/aisdata.ais.dk/ — one label, cert matches, HTTPS works and captive portals cannot intercept it. Also fixed my own defect: verify_env.sh used curl -s, which silences errors as well as progress, making a TLS failure look like an empty page. Patched to -sS in 6 places. | BLOCKED: file naming scheme, per-file sizes and date coverage still unmeasured. Venue line-of-sight still unknown.
2026-08-27 16:05 | setup | DMA S3 bucket enumerated over path-style HTTPS. MEASURED: two naming schemes coexist — monthly archives under year prefixes for older data (2006/aisdk-2006-03.zip, 900 MB; 2006/aisdk-2006-04.zip, 5194 MB) and DAILY files at bucket root for recent data (aisdk-2026-04-19.zip 643 MB, -04-20 634 MB, -04-21 751 MB, -04-22 827 MB, -04-23 728 MB). A README exists at key "!_README_information_CSV_files.txt". Listing truncated at 1000 keys (IsTruncated true) so later 2026 dates exist beyond the first page. | DECISION FORCED: ~700 MB/day zipped, several GB decompressed, means the hackathon CANNOT stream this on demand. One day must be pulled on a good link BEFORE Friday and immediately subset to a bbox + time window; the raw zip must never enter the repo. Venue wifi on Friday will be contested by the whole hackathon — treat tonight's hotel link as the last reliable download window.
2026-08-27 16:30 | setup | Bucket fully enumerated: 1127 objects, coverage runs from 2006 monthly archives to DAILY files through aisdk-2026-08-25.zip (894 MB) — data is current to 2 days ago. CSV schema retrieved and written to 02_data/AIS_CSV_SCHEMA.md: 26 columns including Ship type, Length, Width, Size A-D, SOG, COG, Heading — i.e. the CLAIMED side of criterion 3 comes straight out of this file, no enrichment needed. Size A-D cross-check declared Length/Width from a second field, so a dimension spoof must keep two fields consistent. TWO PARSING LANDMINES RECORDED: (1) README shows coordinates as "57,8794" with a Danish decimal comma — must be settled against the real file before configuring any reader, because getting it wrong silently shifts every position; (2) timestamps are dd/mm/yyyy, and pandas defaults to month-first, silently mis-parsing every date where day<=12 with no error raised. Both are silent-corruption failures, not crashes. | NEXT: download one day on a good link tonight, then subset. Venue line-of-sight still unknown.
2026-08-28 (Fri, pre-event) | data | T1 DATASET SECURED. aisdk-2026-08-25.zip downloaded to 02_data/raw/, 894,129,645 bytes matching S3 Content-Length exactly, unzip -t reports "No errors detected" — CRC-verified despite an accidental concurrent double-download (two curls to one path; resume + CRC test proved the prefix was clean). Contains aisdk-2026-08-25.csv. .gitignore written to keep raw data, .venv, *.pt and env files out of any commit. 02_data/subset_ais.py written: streams the day in 500k-row chunks, cuts three candidate areas (great_belt, fehmarn_belt, bornholm) in one pass, auto-detects delimiter/decimal from the file itself and PRINTS what it detected, and reports rows + unique MMSIs per area so the demo location is chosen from evidence. | NEXT: unzip, run subset, pick area from counts. STILL OPEN: venue line-of-sight to water (gates T2, expires tonight), event IP terms (gates first commit), 03_src/contracts.py and LIBRARIES.md still empty.
2026-08-28 (Fri, pre-event) | data | SUBSET COMPLETE. 32,661,798 rows scanned from aisdk-2026-08-25.csv (5.88 GB decompressed from 853 MB). Dialect settled FROM THE FILE: delimiter ',' decimal '.' — the README's "57,8794" was Danish locale prose, not the actual format, so landmine 1 is CLOSED. Landmine 2 CONFIRMED REAL: row 1 timestamp is "25/08/2026 00:00:00", i.e. dd/mm/yyyy; pandas would silently swap every date with day<=12. Header first column is "# Timestamp" with a leading hash. SLICES (rows / unique MMSI / msgs-per-vessel): fehmarn_belt 1,198,373 / 337 / 3556 — densest and best track continuity. great_belt 494,466 / 172 / 2875. bornholm 485,449 / 339 / 1432 — MOST vessels but 2.5x SPARSER tracks, consistent with the edge of Danish basestation coverage. | DECISION: fehmarn_belt is the T1 demo area — best track continuity, and the Fehmarnbelt corridor carries real seabed infrastructure so the cable-proximity narrative is honest. Bornholm retained as the narrative comparison, NOT the working demo. IMPORTANT FOR CRITERION 4: Bornholm's sparse tracks are a false-positive source — an AIS gap caused by coverage is not an AIS gap caused by a switched-off transponder. Distinguishing the two, and saying so, is exactly the honest-limits point criterion 4 asks for. Make it a pitch slide, not a footnote. | ENVIRONMENT VERIFICATION CLOSED — all items PASS. Remaining pre-build gaps are NOT technical: venue line-of-sight (gates T2), event IP terms (gates first commit), contracts.py + LIBRARIES.md (gate the first line of code).
2026-08-28 (Fri, pre-event) | ARCH | CONTRACT GATE CLOSED. Files placed and verified on disk: 03_src/contracts.py (20509 B), 03_src/LIBRARIES.md (6948 B), 00_brief/CONTRACTS.md (8653 B), FILE_OWNERSHIP.md (9829 B, rewritten to lanes A/B/C/D + ARCH, supersedes the EO/AIS/FUSION/DECISION/PITCH draft), tests/{conftest,fixtures,test_contracts}.py. All four python files parse. 37 test functions = 49 test cases (two are parametrized over all seven models); VERIFIED 49 passed against pydantic 2.13.3 before transfer. Seven boundary types, pydantic v2, frozen, extra=forbid, ZERO logic. Structural guardrail encoded as a test: Verdict has no field capable of holding model-generated text, so the deterministic-verdict / LLM-rationale split cannot be silently broken. Import convention settled in tests/conftest.py: 03_src has a leading digit and is not a legal package name, so sys.path + flat imports. .gitignore rewritten and CONFIRMED on disk — the previous version un-ignored 02_data/slices/ via a negation and fehmarn_belt.csv is 248 MB, over GitHub's hard 100 MB per-file limit; the first push would have been rejected. Now also excludes the two files referencing the restricted architecture, plus .DS_Store. | NOT YET VERIFIED: the five new packages (scikit-learn, folium, pytest, loguru, openai) were installed by Amol but the version report was not captured — the agent shell is a Linux VM and cannot see the Mac's venv. Run pytest on the Mac to close this. STILL OPEN: venue line-of-sight to water, event IP terms.
2026-08-28 (Fri, pre-event) | ARCH | AFFILIATION REMOVED. This project is NOT affiliated with any company. All 24 references across 9 files rewritten; recursive case-insensitive sweep of the repo (excluding .venv and bulk data) now returns ZERO matches in file contents and ZERO in filenames. Sentences were rewritten whole rather than word-deleted — a blank where a name was is worse than the name, because a reader notices the hole. Files touched: CLAUDE.md, STATUS.md (path reference only, no ledger line removed), .gitignore, FILE_OWNERSHIP.md, 03_src/LIBRARIES.md, 99_scratch/verify_env.sh, 00_brief/challenge_statement.md, 00_SHARED_macOS_Demo_Architecture.md, EDTH_Hamburg_Topic02_COMPLETE.md. IP and disclosure disciplines UNCHANGED — "no proprietary code before IP terms" and the prior-work non-disclosure both still stand; only the company name is gone. CRITICAL BUG FOUND AND FIXED IN PASSING: the large project document had already been renamed to EDTH_Hamburg_Topic02_COMPLETE.md, dropping a company-name prefix, but .gitignore still listed the OLD prefixed filename — so that document, which references the restricted architecture, was NOT being ignored and would have gone into the first commit. .gitignore now names the current file. | ACTION REQUIRED BY AMOL, NOT DONE HERE: (1) rename the secrets directory from the old company-named path to ~/.config/edth-hamburg (it is outside the mounted project folder, so the agent cannot reach it); docs and verify_env.sh already reference the new path, so the env file will not be found until the rename happens. (2) Edit the Cowork PROJECT INSTRUCTIONS, which still carry the company framing — the agent cannot change those, and they will otherwise reintroduce the affiliation into future sessions.
2026-08-28 | B | eo_detector.py written (888 lines): 3 sources (camera/video/image dir), ultralytics mps + COCO 'boat' by name, ByteTrack for track_length_frames, pinhole bearing + quadrature sigma, JSONL + run manifest, benchmark CLI. Bearing maths unit-verified. BLOCKED on ARCH: EoContact needs detection_confidence (request filed 99_scratch/requests.md; patch in handoff_B.md). FPS and detection counts NOT MEASURED - no clip in 04_demo/ and the agent shell has no Metal/camera/ultralytics. Paste-ready measurement block in 99_scratch/handoff_B.md.
2026-08-28 14:48 | A | LANE A DELIVERED. 03_src/ais_ingest.py: one AisSource interface, two implementations — DmaCsvSource (recorded DMA bulk CSV, pandas) and NmeaLiveSource (live AIVDM over TCP, pyais TCPConnection + AISTracker doing the static/dynamic merge). No NMEA parser written; the pyais 3.2.1 API was read from source and exercised offline before use, not guessed. Also written: 02_data/INVENTORY.md (measured counts only), 02_data/make_golden_window.py, tests/test_ais_ingest.py (24 tests, one per silent-failure mode), DATASETS.md licence table. FIVE PROPERTIES OF THE REAL DATA MEASURED AND HANDLED, every one a silent-corruption risk: (1) 761,196 of 1,198,373 rows (63.5%) are DUPLICATE RECEPTIONS — several DMA basestations hear one transmission and DMA writes a row each; unhandled, every reporting rate inflates 2.75x and lane C sees three identical association candidates per hull. 431,751 vessel rows / 321 MMSIs survive. (2) 16 MMSIs / 10,972 rows are AtoN — buoys, not vessels; a claim with no possible observation, i.e. an eternal position spoof. Excluded and counted. (3) DMA has ALREADY decoded the ITU sentinels — measured ZERO occurrences of heading 511, SOG 102.3, COG 360.0 — but pyais delivers them RAW on the live path (verified offline: speed 102.3, course 360.0, heading 511, turn NO_TI_DEFAULT). Both paths null them; the CSV path counts them so a future export that stops pre-cleaning shows as a counter moving, not as data quietly changing meaning. (4) "Unknown"/"Undefined" are STRINGS not nulls — 295,344 IMO rows and 17,370 Callsign rows — and pass a naive reader straight into an evidence record as a claimed IMO of "Unknown". (5) dd/mm/yyyy confirmed against row 1; single parse site, explicit format. SHIP-TYPE POLICY DECIDED: conservative. 355k rows (29.6%) carry service-craft types (HSC, Pilot, SAR, Port tender, Law enforcement, Dredging, Diving, Other) with no honest VesselClass equivalent; they map to "unknown" = "claim made, not comparable to a silhouette", NOT to whichever class they resemble. Mapping by resemblance would manufacture SPOOF verdicts inside lane A, invisible to the report, against real named hulls. LICENCE RECORDED IN FULL: Danish act 596/2005 — no warranty; no combining with other datasets to identify individuals without Danish DPA authorisation; REDISTRIBUTION NOT ADDRESSED. Consequence: committing raw DMA rows to a public repo takes an unforced position on ungranted terms AND breaches the hard rule on naming real vessels. Lane A ruling: only PSEUDONYMISED golden windows are committed (999-prefixed synthetic MMSIs — MID 999 is unassigned to any country, so it is recognisably fake; the real files and the MMSI map stay local). GOLDEN WINDOWS SPECIFIED AND MEASURED FROM THE SLICE: fehmarn 2026-08-25 10:00-11:00 UTC = 22,528 rows / 139 vessels / 4,576,253 B — the demo window, an hour rather than ten minutes because criterion 2 ranks BEHAVIOUR and nothing loiters, alters course or goes quiet in ten minutes; 10:10-10:20 = 3,703 rows / 121 vessels / 750,125 B nested inside it as the fast iteration loop. | BLOCKED / OPEN: (1) .gitignore has NO rule for the new 02_data/golden/ directory, so the first git add would commit real vessel identities — the same failure mode as the 02_data/slices/ negation caught this morning. Filed to ARCH; must be fixed BEFORE the first commit, and the rule must be 02_data/golden/*.csv with a !*_anon.csv negation, NOT the directory, because git cannot re-include a file below an ignored directory. (2) pytest and make_golden_window.py NOT YET RUN — the agent shell is a Linux VM and pydantic's compiled core will not import there, so nothing that constructs an AisTrack has been executed. Paste block with expected cross-check numbers is in 99_scratch/handoff_A.md. (3) LIVE AIS FEED LICENCE NOT READ — NmeaLiveSource is written and ready but T2 has no legal data source until DATASETS.md's live-feed row is filled. (4) DMA timestamps ASSUMED UTC, not verified; if they are local, every timestamp is 2 h off in August and every time_delta_s inherits it. (5) Four cross-lane requests OPEN in 99_scratch/requests.md: "unknown" must suppress the class comparison (C); one MMSI collapses to one track_id so kinematic track-breaking is needed for the identity-spoof case (C); an AIS gap alone must not reach DARK, because a bbox exit and a coverage hole are indistinguishable from a switched-off transponder in this file (C, D); .gitignore (ARCH).
2026-08-28 21:30 | ARCH | REPO PUBLISHED + HARD DEMO CONSTRAINT ACCEPTED. Pushed to github.com/amol-jatayu/maritime-approaches, 37 files / 147 KiB, branch main. Bulk AIS, .venv, weights and the two RESTRICTED docs excluded and verified excluded before the first commit. Auth note: account is Google SSO, so git needs a classic PAT with `repo` scope, not a password; a failed attempt caches a bad macOS keychain entry that must be erased before retry. README.md written — the repo previously had no entry point, so a teammate cloning it got 37 files and no explanation; README now carries the claimed/observed wall, the four verdict paths, the DMA fetch commands with the path-style-S3 gotcha, the five silent-corruption properties, and the module status table. NEW HARD CONSTRAINT FROM AMOL: no water access of any kind, venue is indoors. T2 CANCELLED. 04_demo/DEMO_INDOOR.md written with the D0/D1/D2 ladder, the injected-fault table, the demo script and the mandatory honesty slide. IMPORTANT: this costs NOTHING in 03_src — association/consistency/verdict/prioritizer/evidence are pure functions of EoContact + AisTrack, so only the PRODUCER of EoContact changes. It also kills the unread live-AIS-feed licence blocker, which was gating T2 only. | NEXT, IN ORDER: (1) 04_demo/make_synthetic_eo.py — the D0 harness, blocks everything downstream; (2) consistency.py (criterion 3); (3) verdict.py; (4) prioritizer.py (criterion 2, the differentiator); (5) evidence.py + report.py (criterion 4). STILL OPEN: event IP terms; pytest never run on the Mac; DMA timestamps assumed UTC, unverified.
2026-08-28 15:40 | A | TRAJECTORY ANALYSIS DELIVERED. New 03_src/ais_trajectory.py (835 lines) and tests/test_ais_trajectory.py (16 tests). ais_ingest.py UNCHANGED and still imports only pandas + contracts — the geo stack is confined to the new module on purpose, so a teammate with a broken geopandas install can still read AIS. NOTHING HAND-ROLLED: movingpandas TrajectoryCollection for per-vessel tracks, Trajectory.add_speed/add_direction/add_timedelta for the speed profile, TrajectoryStopDetector for loiter, ObservationGapSplitter exposed for lane C, geopandas reprojection + shapely for distance. All APIs read from source in the venv before writing against them (movingpandas 0.23.0, geopandas 1.1.4, shapely 2.1.2). MEASURED, Fehmarn slice full 24 h, 431,745 deduped reports / 315 vessels: AIS GAPS 3,265 over 5 min, 660 over 10 min across 108 vessels, 333 over 15 min, 146 over 30 min, 60 over 60 min; gaps over 10 min have median 15.0 min, p90 55.5 min, max 17.6 h. These are exact — the same t.diff()>gap arithmetic movingpandas itself performs, computed independently with pandas, so they double as the cross-check for the module run. LOITER: geometric count PENDING the Mac run (shapely/geopandas cannot load in the agent's Linux VM). Claimed-SOG proxy measured instead and labelled as a proxy, not the answer: 293 stationary periods >=30 min across 117 vessels, of which 224 do NOT declare a stationary navigational status; 92 of those claim "Under way using engine" while reporting SOG < 0.5 kn for over half an hour. Top ship types among the non-declaring set are Tug 71 and Sailing 65 — the tunnel construction fleet and becalmed yachts. THAT TABLE IS THE CRITERION 2 PROBLEM IN ONE PLACE: a bare stop count is a list of harbour furniture, so every BehaviourEvent carries the claimed nav statuses seen during it and every gap event carries explained_by_area_exit (last position within 2 km of the slice boundary, measured in metres via ETRS89/UTM32N, not degrees) and explained_by_sparse_coverage (this vessel's own median interval > 3x the area median). Neither flag is a verdict; both are suppressors for lane C. THREE LIBRARY TRAPS FOUND BY READING THE SOURCE, each one a wrong number rather than a crash: (1) TrajectoryCollection.__iter__ RAISES on any trajectory with fewer than two points and the slice contains six single-report vessels, so build_trajectories floors at min_points=2; (2) ObservationGapSplitter discards segments with fewer than two points, so a lone report between two silences is dropped and the two gaps MERGE — counting segments would under-report gaps, worst exactly where coverage is sparsest, so detect_gaps reads the timedelta column instead and there is a regression test for it; (3) geopandas .distance() aligns on INDEX by default, which yields NaN rather than an error on a mismatched index, so pairwise_distance_m passes align=False. Also: TrajectoryStopDetector.max_diameter is METRES on a geographic CRS (geodesic via geopy), not degrees — a degree value would search a box hundreds of km wide. Thresholds are treated as arguments, not facts: every event carries the detector parameters that produced it, and gap_sensitivity()/loiter_sensitivity() emit the full sweep so a count is never quoted without its threshold. | BLOCKED / OPEN: (1) CONTRACT INTERRUPT filed to ARCH — contracts.py needs a new frozen BehaviourEvent type, EvidenceRecord.behaviour_events, and three additive optional AisTrack fields under a THIRD prefix implied_ (implied_speed_ms, implied_course_deg_true, gap_before_s). Rationale: a loiter is a SPAN and AisTrack is a per-report snapshot, so stamping it per report destroys the start/end/centroid an operator needs; and separately, keeping implied speed apart from claimed_sog_kn is what makes the camera-free kinematic self-contradiction test possible — a vessel claiming SOG 0.2 kn while its own positions move it 400 m/min has contradicted itself, which is criterion 3 evidence available on recorded data with no EO frame. Merge the two fields and that check is arithmetically erased. Lane A ships a local mirror dataclass meanwhile so C and D are not blocked; the swap is an import change. (2) pytest and the report run NOT YET EXECUTED — paste block with expected cross-check numbers is in the handoff_A.md addendum. (3) Geometric loiter count still unmeasured; only the SOG proxy exists. (4) New files 03_src/ais_trajectory.py and tests/test_ais_trajectory.py need a line in FILE_OWNERSHIP.md §2 under lane A.
2026-08-28 | B | geometry.py written (1013 lines) + eo_detector refactored to import from it. Exact ray model (de-roll, pitch, 3-D norm), horizon dip, spherical range by bisection on a stable forward map, asymmetric error band by perturbation with a per-assumption breakdown, pyproj Geod.fwd inverse projection (association.predict_measurement NOT duplicated). VERIFIED in the Linux VM against hand-computed values: horizon matches the mariner's rule 2.08*sqrt(h) to 0.09% at 5 heights; range round-trips to 6e-7; bisection agrees with the closed form to 5e-7; scale invariance exact (10.26% rel sigma at both h=20m/s=200m and h=0.3m/s=3m). HEADLINE MEASURED FINDING: at 20 m height on a 70 deg lens this camera cannot range past ~1.1 km - 8.5 km and 16.7 km differ by 0.8 PIXELS of waterline. Marking the horizon in-frame beats an inclinometer 1.7x and moves usable range 675 m -> 1125 m. Flat-earth UNDERestimates range (-27% at 0.15 deg depression), opposite to the common intuition. pyproj wrappers NOT executed here (no pyproj in the agent VM) - Amol runs the paste block. Request filed to C re: duplicated shortest-arc helper.
2026-08-29 | C/D | PIPELINE COMPLETED END TO END, 2572 NEW LINES, NONE OF IT EXECUTED. Written: consistency.py (529, per-dimension claimed-vs-observed in SIGMAS with five suppression rules), verdict.py (409, MATCH/DARK/SPOOF/UNKNOWN + calibrated confidence capped at 0.97), prioritizer.py (339, criterion 2, weighted score incl. an ACTIONABILITY term so a 9-sigma spoofer two hours away ranks below a 5-sigma loiterer over a cable), evidence.py (256, case file + DETERMINISTIC rationale template so the whole project can run with no model at all), 04_demo/run_pipeline.py (273, sequences every stage AND scores itself against the harness ground truth), 04_demo/app.py + web/index.html (operator screen, Python stdlib only, ZERO network dependencies — hand-drawn SVG plan view, no Leaflet, no CDN, runs with wifi off), 04_demo/pi_sensor.py (412, CLASSICAL CV sensor node for the Raspberry Pi — MOG2 background subtraction + connected components + nearest-centroid tracking, NO neural network, needs only cv2 and numpy), 04_demo/make_demo_scenario.py + demo_scenario.csv (8 vessels, 488 rows, 89 KB, FULLY SYNTHETIC — no DMA rows, no licence question, committed so a fresh clone runs with zero downloads). KEY DESIGN RULINGS: (1) the position-spoof / missed-detection distinction is resolved by ORPHAN CORROBORATION — a real position spoof leaves TWO orphans (a displaced claim AND the real hull), a missed detection leaves one; that single rule is what keeps the false-positive controls silent. (2) Range MUST come from waterline depression geometry, never from apparent size, because range-from-assumed-length makes observed_length collapse to the claim and the length check can never fire. (3) Classical CV chosen over YOLO for the Pi: it removes the D1 kill risk (COCO 'boat' may not fire on paper silhouettes; background subtraction does not care what the object is), runs at 30 FPS on a Pi where YOLO crawls, and is far more defensible in a case file. Cost: no silhouette class, so class-spoof needs the VLM; length/heading/position spoofing all work with zero AI. (4) .gitignore fixed for 02_data/golden/ BEFORE make_golden_window.py is ever run — it had no rule, and the first git add would have committed real vessel identities plus the MMSI mapping table. | BLOCKED: NOTHING HAS BEEN EXECUTED. The agent shell is a Linux VM with no pydantic, numpy, scipy or cv2. All 2572 lines parse and nothing more than that is known. Amol must run the smoke sequence on the Mac; expect import errors and contract-validation failures on first run, not a clean pass.
2026-08-28 16:55 | A | PLAUSIBILITY CHECK DELIVERED, inside 03_src/ais_ingest.py — NO new modules, as instructed. Added PlausibilityChecker (streaming, one anchor per MMSI, so it works unchanged on the live T2 path), PlausibilityMismatch, LandMask, PlausibilityStats, anonymise_mmsi(), plausibility_report() and a __main__ entry point; 13 tests appended to tests/test_ais_ingest.py. THE LAZY-IMPORT RULE SURVIVED AND IS NOW ENFORCED BY A TEST: geopandas/shapely/pyproj/movingpandas/pyais are imported inside the functions that need them, and test_reading_ais_still_does_not_require_the_geo_stack parses the module's own AST to assert none of them is a top-level import — reading a CSV still needs pandas alone. MEASURED, Fehmarn slice full 24 h: 41 findings across 27 vessels — 17 zero_interval_jump, 15 impossible_speed, 9 teleport; 278,934 pairs evaluated, 151,998 skipped inside the minimum interval, 243,278 below the 100 m jump floor. THE FINDING THAT SET THE DESIGN: the NAIVE version of this check — implied speed between consecutive reports, no interval floor — produces 705 findings across 146 vessels, and 564 of them (80%) are over dt <= 2 s with a MEDIAN JUMP OF 57 METRES. 57 m in one second is 111 knots and is also the ordinary scatter of a consumer GPS fix, so at 1 Hz reporting the naive check measures the receiver rather than the ship. Sensitivity on the minimum sampling interval: 0s 705, 2s 345, 5s 50, 10s 19, 30s 6. Shipping the naive version means 96% of what an operator sees is noise, which is how a decision-support tool trains its user to ignore it. THE ANCHOR RULE is the fix and is subtle enough to have its own test: a report closer than min_interval_s is skipped WITHOUT advancing the anchor, so a 1 Hz vessel is compared against a base ten seconds back rather than either being measured over one second (noise) or never checked at all (if the anchor advanced). ZERO-INTERVAL TRAP HANDLED: 956 consecutive pairs share a timestamp exactly, because dedupe is on (Timestamp, MMSI, Lat, Lon) and two basestations decoding one transmission to slightly different coordinates both survive; dt=0 makes implied speed infinite, so unhandled these are 956 phantom teleports plus a ZeroDivisionError waiting for whichever consumer divides first. 583 of the 956 differ by under 5 m (rounding) but 21 differ by MORE THAN 100 m in the same second — not rounding, and the leading explanation is TWO VESSELS SHARING ONE MMSI, i.e. the identity-spoof case criterion 3 calls the discriminator. HONEST READING OF THE RESULT, on the pitch slide as well as here: 26 of the 41 findings are on vessels claiming ship type "Sailing" and every worked example is Class B, so the leading explanation is a cheap consumer GPS, not deception. This is a DATA-QUALITY signal; lane C may promote it to SpoofSubtype="kinematic" only with corroboration. COASTLINE RESOLVED AND LICENCE RECORDED: EEA coastline for analysis (polygon) v3.0 March 2017 — CC-BY 4.0, copyright European Environment Agency, "no limitations to public access", attribution REQUIRED, 1:100,000 MMU, native EPSG:3035, lineage EUHYDRO + GSHHG, download https://sdi.eea.europa.eu/data/9faa6ea1-372a-4826-a3c7-fb5b05e31c52 . This is the ONLY dataset in the project with explicit permissive reuse terms — unlike the DMA AIS it can be cited on a slide without hedging. REJECTED the obvious shortcut: geopandas 1.1.4 removed its bundled Natural Earth data and the only shapefile otherwise on disk is a 1:110,000,000 fixture inside pyogrio's test folder, whose coastline error is kilometres in an 18 km strait — it would report every vessel near Rødbyhavn and Puttgarden as sailing overland, i.e. false positives aimed precisely at the vessels closest to the infrastructure. A wrong coastline is worse than none, because a missing one is visible and a wrong one is not. Land is eroded inward 250 m before testing (at 1:100,000 a vessel alongside a quay legitimately falls inside the land polygon) and the check refuses intervals over 60 s (the straight line across a three-hour silence crosses Lolland for every vessel that rounded it). | BLOCKED / OPEN: (1) SECOND CONTRACT INTERRUPT filed to ARCH — Mismatch cannot honestly carry this finding: association_id is required but nothing was paired, and observation_confidence is required but nothing observed it. Requested: add "position_implausible" to MismatchDimension, make association_id optional, add comparison: Literal["claimed_vs_observed","claimed_vs_implied"] defaulted so nothing existing breaks, and add implied_value/implied_field. The discriminator is the load-bearing part — without it the only home for an implied speed is observed_value, and a consumer reading observed_value weights the verdict with an EO uncertainty that was never computed, silently. Lane A ships PlausibilityMismatch as a local mirror with a test asserting it has no observed_* field and no observation_confidence, mirroring ARCH's own guardrail test on Verdict. (2) CROSSES_LAND NEVER RUN — the EEA coastline is not on disk and shapely cannot load in the agent's Linux VM, so one of the three requested checks has no count. Download step is in the handoff_A.md addendum; 02_data/coastline/ also needs a .gitignore rule from ARCH. (3) pytest and the report entry point NOT YET EXECUTED; cross-check numbers are in INVENTORY.md §4.11 and were computed with geopy, the same WGS84 geodesic pyproj uses. (4) Filed to C and D: a position_implausible finding must not reach a SPOOF verdict alone — promoted without corroboration it puts the word "spoof" against a real named yacht.
2026-08-29 | B | eo_vlm.py written (1118 lines): Featherless vision via the openai SDK with base_url, base64 JPEG crops, BoundedSemaphore(2) for the 2-of-4 unit budget, temperature 0, versioned closed-schema prompt. THE HARD RULE IS ENFORCED IN CODE, not prompted: closed enum schema (no free-text field exists) plus a raw-text scan that REJECTS decision language, absolute sizes and identity claims; rejections keep the raw text for audit. Never writes observed_length_m - consistency.check_length names the circularity trap and puts the guarantee on lane B. Confidence capped at the Wilson 95% lower bound of MEASURED agreement (0.95 uncalibrated = 1.645 sigma, below lane C's 2.0 floor), so the uncalibrated state is structurally inert. VERIFIED offline: 17 adversarial outputs all rejected with the right reason, 6 false-positive cases pass, concurrency peak measured at 2 over 12 contacts, apply_to_contact writes exactly 2 fields. Found and fixed by test: the 9-digit MMSI regex was firing on a confidence of 0.123456789. MEASURED CONSEQUENCE: a perfect 20-crop sample licenses only 0.8389 (0.990 sigma); ~165 crops at 100% agreement are needed to reach the 0.9772 firing threshold, and confusable pairs can NEVER fire. AGREEMENT RATE NOT MEASURED - no images exist in the repo, no Featherless key or network in the agent VM, openai not in .venv. Paste block parts 5-6 in handoff_B.md.
2026-08-29 | ARCH+B | detection_confidence APPLIED to contracts.py as a REQUIRED field (ge=0, le=1), on Amol's explicit in-session authorisation. Also patched: tests/fixtures.py (+1 line) and 04_demo/make_synthetic_eo.py (new _synthetic_detection_confidence helper + both construction sites) - the lane D edit is disclosed in 99_scratch/requests.md. Synthetic confidence is a saturating function of APPARENT PIXEL WIDTH with the SAME rule for real vessels and clutter, deliberately: a hand-placed low value for clutter would let anything downstream separate the two trivially and would make the scene's false-positive rate optimistic invisibly. Clutter 6-22px lands 0.43-0.58; a real 12px vessel lands 0.49 - overlapping on purpose. All 9 static checks pass (field required+bounded, all 4 construction sites pass it, eo_vlm inherits via model_dump). NOT RUN HERE (no pydantic in the agent VM): pytest, and the scene01 regeneration. 04_demo/out/scene01/eo_contacts.jsonl is STALE and will raise in run_pipeline.py:73 and app.py:143 until regenerated. eo_detector.py's pending-contract guard is now satisfiable - the EO path is unblocked.
2026-08-29 | C/D | FIRST END-TO-END RUN ON THE MAC, AND IT FOUND TWO PIECES OF DEAD LOGIC. Pipeline executed clean: 8 vessels -> 5 claims + 9 contacts -> 5 matched, 4 dark candidates, ranked list and app all working at 127.0.0.1:8000. Scoring: 5/7 injected faults caught, 2/3 controls violated. FOUR DEFECTS DIAGNOSED AND FIXED, none of which any crash-based test could have found. (1) THE CLASS CHECK COULD NEVER FIRE: significance used the normal quantile, mapping 0.92 confidence to 1.41 sigma, permanently below the 2.0 reporting threshold -- the check ran on every pair and was mathematically incapable of producing a finding. Replaced with log-odds ln(p/(1-p)); severity for a NON-confusable class disagreement at >=0.85 confidence is now promoted to major on domain grounds, because severity is a domain judgement while significance is a statistic and for a categorical dimension they must be set separately. (2) THE POSITION CHECK COULD NEVER DETECT A POSITION SPOOF: measured at 5 km, cross-range (bearing) sigma is 218 m and along-range (monocular range) sigma is 1250 m; pooling them buried a 1000 m spoof at 0.79 sigma when the cross-range-only test puts it at 4.58. Rewritten to compare predicted bearing of the claim against observed bearing. HONEST LIMIT NOW STATED IN THE CODE AND ON THE SLIDE: a claim displaced ALONG the line of sight is undetectable by one monocular sensor at any offset, ever -- that needs a second baseline, a radar, or the vessel to move. (3) CLUTTER SCORED AS DARK: a single-frame blob was being labelled DARK, which asserts a VESSEL is present and not transmitting -- a blob does not support the first half of that sentence. Persistence is now a PRECONDITION for the label, not a confidence penalty; below it the output is UNKNOWN. (4) Cross-dimension corroboration added to verdict: two independent dimensions disagreeing reach SPOOF where one minor cannot, which is also what stops class-alone from ever convicting. ALSO: control scoring now distinguishes ACTIONABLE false positives (labelled AND not deferred) from labelled-but-deferred, and reports both so the definition cannot be self-serving; prioritizer gained a BEHAVIOUR route to recommendation, because an honest vessel loitering over a cable was ranking first and then not being recommended, which is incoherent; demo scenario widened from 8 to 16 vessels (5 wide of the wedge, 3 beyond max range) because the harness warned it had no out-of-view vessel to build a coverage-hole control from, and a scene with no controls cannot measure a false-positive rate. README gained a full decision register: every technique against the alternative it was chosen over, plus an honest inventory showing six of eight stages contain no model at all. | NEXT: re-run the three demo commands and confirm the four fixes land. The class and position rows should flip to ok; both clutter rows should become UNKNOWN.
2026-08-29 | ARCH | CONTRACTS EXTENDED FOR THE LIVE CONSOLE — section 8 APPENDED, sections 1-7 untouched (no rename, no retype, no reorder), verified by diff against the pre-edit copy. New: SensorNode (node_id/kind/online/last_seen/measured_fps), OperatorAction (DISPATCH|WATCH|DISMISS|EXPORT + operator_id + action_time_utc + optional note), five StreamEvent variants under a pydantic DISCRIMINATED union on an `event` tag (contact_update, verdict_update, queue_update, node_status, operator_action_ack), and ConsoleState. TWO REQUESTED ITEMS WERE ALREADY SATISFIED AND WERE DELIBERATELY NOT ADDED: PriorityScore already breaks out per factor via component_scores + component_weights, so a `factors` dict would be a SECOND source of truth for why one contact outranks another and the failure is silent — the console would render one while the prioritizer computes from the other, explaining a ranking the pipeline never produced; a test now FORBIDS factors/breakdown/reasons/contributions so that decision survives. Verdict.defer_to_human was already present and required. FOUR DESIGN RULINGS: (1) every event carries a monotonic gap-free `seq` and ConsoleState carries `last_seq`, because a WebSocket cannot promise delivery and a lost queue_update otherwise leaves a stale ranking on screen with nothing raising anywhere — the client applies the snapshot, drops seq <= last_seq, resumes at last_seq+1; without it a mid-pitch browser refresh shows an empty page and the only recovery is re-running the pipeline on stage. (2) THE CLAIMED/OBSERVED WALL HOLDS ON THE WIRE: contact_update carries ais_track and eo_contact as two separate optional fields, never a merged "vessel" — a UI-shaped merge is the easiest thing here to write and deletes criterion 3 from the product without raising. (3) DISCRIMINATED, not a plain union: pydantic accepts the first union member that validates, so a node_status body wearing a queue_update tag would be accepted as the wrong type and update the wrong panel with no error; the tag is now authoritative. (4) The ack means RECORDED, never EXECUTED — OperatorActionAckEvent has `accepted: bool` and structurally no field for an outcome, the same guardrail style as Verdict having nowhere to put LLM text. SensorNode.last_seen and measured_fps default to None: substituting "now" for a never-heard node renders a dead sensor as healthy, and a nominal 30 next to a node delivering 4 breaks MEASURED NUMBERS ONLY. contracts.py stays logic-free and transport-free, now ENFORCED BY TWO AST TESTS: zero function/lambda definitions, and imports restricted to {typing, pydantic} — lane F is the reason, since one FastAPI import there stops the Pi booting with an ImportError pointing at a file nobody suspects while the shore station keeps working. FILE_OWNERSHIP.md: lanes E (console — 04_demo/app.py, 04_demo/web/) and F (edge node — 04_demo/pi_sensor.py, 04_demo/camera_pose_*.json) added and CARVED OUT of lane D's 04_demo/ blanket, with the dependency diagram, the lane-letter list and the assignment table updated to match. tests/test_contracts.py: 49 -> 104 test cases, ALL PASSING. | HONEST LIMIT ON THAT PASS: the 104 were run in the CLOUD CONTAINER (Linux, python 3.11.15, pydantic 2.13.3, pytest 9.0.3) against a staged copy of contracts.py + tests/, NOT on the Mac — device_bash is the network-less Linux VM with no pydantic and cannot execute .venv/bin/python. Same source, different interpreter. Both files also py_compile cleanly in the VM. AMOL MUST RUN `python -m pytest tests/ -q` ON THE MAC to confirm against pydantic in .venv and to cover test_ais_ingest.py and test_ais_trajectory.py, which the container cannot run (no pandas/geo stack there). NOT DONE HERE: 04_demo/app.py and web/index.html do not yet emit StreamEvent or ConsoleState — the contract exists, lane E has not been wired to it; pi_sensor.py does not yet report a SensorNode. Both are lane E/F work and neither is blocked.
2026-08-29 | F | HARDWARE.md CREATED, ALL HARDWARE MEASUREMENTS STILL UNMEASURED — the agent cannot reach the Pi (its Mac shell is the network-less Linux VM; the cloud container is a different machine; the Pi is on neither path), so this session produced the INSTRUMENTS, not the readings: 99_scratch/probe_pi.sh (identity / camera / resolution-vs-throughput / network, self-interpreting PASS-FAIL, defensive about the picamera2 API — hasattr/getattr rather than assuming a version) and 99_scratch/probe_pi_mac.sh (MAC_IP, bind address, firewall, reverse ping). Both bash -n clean; both embedded python blocks ast.parse clean; NEITHER EXECUTED. THREE FINDINGS NEEDED NO PI. (F-1, P0, DEMO KILLER) app.py:169 --host defaults to 127.0.0.1 — loopback, unreachable from the Pi under any network configuration, and it fails as a timeout that is indistinguishable at the Pi from a cable fault. No code change needed, the argument exists: --host 0.0.0.0. Corollary: the first non-loopback bind raises the macOS incoming-connections dialog and a dismissed dialog drops every POST silently. (F-2, P0) pi_sensor.py:349 uses cv2.VideoCapture for the CSI camera, against the Lane F brief; the dangerous case is not the open failure but a USB webcam resolving to index 0, which runs perfectly while computing every bearing from the OTHER lens's hfov_deg — silent corruption, same class as dd/mm/yyyy. Filed to lane D; interim mitigation is to run with no USB camera attached so the loud failure is forced. (F-3) picamera2 sensor_modes are NOT output sizes: setting resolution by selecting a cropped mode narrows the HFOV and silently invalidates hfov_deg, which floors every bearing sigma and caps every SPOOF confidence — set main= only, re-survey HFOV after any resolution change. RESOLUTION DERIVED AS FAR AS POSSIBLE: min_area_px=120 at aspect 2.8 gives a floor of k=18.3 px blob width, but make_synthetic_eo places CLUTTER at 6-22 px, so a silhouette at the floor sits INSIDE the clutter band and persistence becomes the only discriminator — operate at ~3k=55 px. W = k*2*tan(HFOV/2)*d/L, and the useful reading is by column: resolution is the expensive lever (MOG2 is per-pixel, 640->1280 is 3x the work) while d/L is free — 100 mm at 1.5 m needs 1071 px at 66 deg, 200 mm at 1.0 m needs 357 px. PRINT BIGGER AND MOVE CLOSER BEFORE RAISING RESOLUTION. Provisional 640x480 (already pi_sensor.py's default) conditional on d/L<=5-8 and a 66-75 deg lens; a Wide 102-120 deg variant breaks 640 at any sensible table distance. The README's '30 FPS on a Raspberry Pi' is NOT measured for this Pi and must not reach a slide until the probe's benchmark table is filled. | BLOCKED: everything in HARDWARE.md §§1-4 pending the two probe runs. camera_pose_TABLETOP.json still has hfov_deg 0.0 and height_m 0.0 and pi_sensor.py:341 refuses to start until hfov_deg is set — it is a SURVEY task (two known bearings at opposite frame edges), not a probe output. Lane F absent from FILE_OWNERSHIP.md (filed to ARCH). BREAKOUT HW-N1 IS UNDEFINED — the brief says run it if Pi and Mac cannot see each other, but no such procedure exists in the repo.
2026-08-29 | E | SHORE STATION DELIVERED, STATICALLY CHECKED, NOT EXECUTED. 03_src/server.py (1311 lines) + 99_scratch/lane_e_server_check.py (10 static checks, 10/10 pass in the agent VM) + 99_scratch/handoff_E.md. Nine routes: / (serves lane D's console), /api/state (COMPATIBILITY — lane D's own State class IMPORTED not copied, so app.py stays an interchangeable fallback rather than a different demo), /ws, /ingest/contacts, /api/contacts (alias — pi_sensor.py's hardcoded default, so the sensor works unchanged), /stream, /action, /evidence/{id}, /health. THE ORCHESTRATION RULE IS ENFORCED BY TEST, NOT BY INTENT: static check 2 asserts server.py does not even IMPORT association/consistency/verdict/prioritizer (it goes through run_pipeline.run_scene, the only tested sequencing of the five stages), and check 3 asserts it never compares a significance, assoc_score, delta or tolerance against anything. The server carries; the pipeline decides. THREE BRIEF ASSUMPTIONS FALSIFIED BY READING THE CODE: (1) pi_sensor.py serves NO video — it is a push client with urllib, no HTTP server, no MJPEG — so /stream relays a configured multipart/x-mixed-replace URL (mjpg-streamer/motion, real and standard) or encodes the local camera, and returns 503 WITH A STATED REASON otherwise, never a placeholder frame, because a still image on a watch screen is indistinguishable from a live one showing calm water. (2) fastapi/uvicorn are NOT in .venv (MEASURED: pydantic and pydantic_core only; venv is --system-site-packages so the framework Python was not visible from the agent shell) — README's stdlib-zero-network ruling still stands and is managed by SEPARATION not argument: app.py is untouched, stdlib, already run end to end, serves the same page and the same /api/state, and _require_web_stack() prints the install command AND the fallback command instead of a traceback. (3) There is no lane E in FILE_OWNERSHIP.md and web/index.html is lane D's polling page with no WS client — server.py is a new file so no ownership was breached, but /ws currently has no consumer. BROADCAST ON CHANGE IMPLEMENTED AS A CORRECTNESS RULE, NOT A UX ONE: re-emitting an unchanged ranking makes "the ranking changed" unobservable, and criterion 2's whole claim is that the order means something. Every emitter is gated on a content fingerprint, and the fingerprints are FIELD TUPLES rather than == on the frozen models — Verdict.decided_at_utc changes on every recompute, so whole-object comparison would emit a verdict_update every pass and the rule would be SILENTLY DEAD WHILE APPEARING IMPLEMENTED. Floats quantised to 3 places (console shows 2) because two runs over identical inputs differ in the 15th decimal. The node watchdog is the one timer and it is not an exception: a node going silent is a real change that nothing arrives to announce — the signal is an ABSENCE and can only be noticed by looking — so the 1 s tick drives DETECTION while the fingerprint still gates EMISSION, and a server beside a dead Pi sends exactly one event, the flip. THE AUDIT TRAIL IS TREATED AS A DELIVERABLE: 04_demo/operator_audit.jsonl, append-only, flushed AND fsynced BEFORE the ack is sent (without the fsync the line sits in the page cache and a laptop losing power leaves the operator believing a decision is recorded when it is not), contract object nested under "action" with server facts outside it, line counter initialised from the existing file so a restart mid-demo does not erase what the operator already decided. accepted MEANS "written to the record" and nothing else; an action on a contact the server no longer holds is written with contact_known=false rather than REFUSED, because refusing it would delete a decision a human made. A DISMISS is recorded, not deleted. /evidence is pseudonymised behind TWO independent gates (--allow-real-identities AND ?allow_real=1) because a URL leaves the machine the moment anyone opens it on the projector. SUBSCRIBE-BEFORE-SNAPSHOT on /ws, deliberately: an event emitted between snapshot and subscription would be lost with NO GAP IN seq to reveal it — a quietly wrong picture, the one failure the counter exists to prevent — whereas subscribing first can only duplicate, and a duplicate is detectable (seq <= last_seq) and discardable. Prefer the recoverable error. A full subscriber queue CLOSES the connection rather than dropping an event, for the same reason. CHECKER DEFECT WORTH READING (handoff_E §5): the static checks failed 3/10 on the first run and ALL THREE were bugs in the checker. Checks 8 and 10 matched raw source — check 8 forbids opening the audit file "w" and the docstring EXPLAINING that rule contains those characters; check 10 forbids a merged "vessel" payload and the comment EXPLAINING why contains that word. A file that documented a rule was reported as breaking it. General form: a substring check over source cannot tell code from prose ABOUT code, so the better-commented the file the more false positives — a check that punishes explanation. Harmless here because loud; the dangerous mirror image is a check that PASSES because the forbidden token sits in a comment. The first fix used a tokenize heuristic ("a STRING preceded by NEWLINE/NL/INDENT is a docstring") which silently ate every continuation line of an implicitly concatenated string inside parentheses, since tokenize emits NL within brackets — checks 7 and 8 then failed for the OPPOSITE reason, the code they looked for had been deleted by the stripper. Docstrings are now located by AST POSITION. | BLOCKED / OPEN: (1) NOTHING IN server.py HAS BEEN EXECUTED — no pydantic, fastapi or browser in the agent VM; expect import errors and contract-validation failures on first run, not a clean pass. Paste block incl. a falsifiable broadcast-on-change test (POST the SAME contacts twice, assert the second produces zero contact/verdict/queue events) is handoff_E.md §6. (2) fastapi/uvicorn presence still UNVERIFIED — step 0 of the paste block is the measurement. (3) /ws has no client until lane D adds one; request filed. (4) /evidence serves NO pairing_basis block because run_scene() does not surface its Association objects — the server refuses to fabricate one since assoc_ambiguous is a defer-to-human trigger and is the one number a console must never guess; request filed to D. (5) 04_demo/out/scene01/eo_contacts.jsonl may still be STALE per the ARCH+B line above — regenerate before trusting any run. (6) FIVE requests filed in 99_scratch/requests.md (ARCH x2 incl. a .gitignore ruling on operator_audit.jsonl, which carries real operator_ids and is the same class of problem as the 02_data/golden/ rule; D x2; B x1 needing no code change).
2026-08-29 | F | EDGE NODE BUILT AND PARTLY VERIFIED; NO PI NUMBERS. Written: edge/sensor_node.py (708, picamera2 + ultralytics YOLO on COCO 'boat' + EoContact + POST + FastAPI MJPEG /stream + /health), edge/bench_edge.py (320, imgsz sweep + 5-min soak + thermals + throttle + detection rate), edge/sync_to_pi.sh, edge/requirements-edge.txt, 03_src/edge_client.py (453), 04_demo/edge_benchmark.md (211), 99_scratch/lane_f_edge_check.py (263). VERIFIED BY EXECUTION in the cloud container (python 3.11.15, pydantic 2.13.3, cv2 4.13.0) with picamera2/ultralytics/fastapi/uvicorn/torch stubbed through sys.modules: 42 checks, 41 PASS. The load-bearing ones: the node's payload validates as a real EoContact over 12 yaw/position combinations; BOX WIDTH DOES NOT MOVE RANGE, which is the check that proves apparent size has not leaked into the range estimate and therefore that the length test can still fire; observed_class stays None while detection_confidence carries the YOLO score; above the horizon range is None rather than a guess and the contact is still valid bearing-only; an injected claimed_mmsi is REJECTED, so the identity wall holds on the wire; the four geometry helpers ARE pi_sensor's objects, so there is exactly one copy of the bearing maths on the Pi; all six rows of edge_client.reconcile(). ARCHITECTURE DECISION (Amol, in session): edge_client.py is a CLIENT, NOT A SECOND SERVER. server.py already owns POST /ingest/contacts, _note_node, the 30 s watchdog and NodeStatusEvent; duplicating that is the PriorityScore.factors failure again (two sources of truth, silent divergence). edge_client polls the node's own /health from the opposite direction and adds the one case server.py STRUCTURALLY CANNOT SEE: a Pi that is powered, detecting and streaming but whose POSTs never arrive is not 'offline' to server.py, it is ABSENT FROM ConsoleState.nodes entirely, so the console draws nothing and an empty queue reads as an empty sea. reconcile() resolves the two views into HEALTHY / UP-BUT-NOT-POSTING / PORT-BLOCKED / DOWN / NEVER-HEARD-FROM, each with a suggested_action; POLL_STALE_AFTER_S is pinned to server.py's 30 s so the two views cannot disagree about staleness. YOLO DOES NOT REPLACE CLASSICAL: sensor_node.py and pi_sensor.py both stand, because COCO 'boat' may not fire on a printed silhouette; edge_benchmark.md §3 states the switch criterion and forbids the tempting wrong move of lowering --conf until something appears. P0 FOUND AND MEASURED WHILE BUILDING: pi_sensor.to_contact() omits detection_confidence, required since this morning, so EoContact(**...) raises 'Field required' and server.py returns HTTP 400 on EVERY post from the classical node — the FALLBACK IS CURRENTLY BROKEN, which matters because the YOLO node's escape route depends on it. Filed to lane D; the regression guard in lane_f_edge_check.py fails on purpose until fixed. FRAME-RATE ARGUMENT DERIVED, not apologised for: a vessel at 12 kn covers 6.17 m/s, so at 2 FPS the inter-frame step is 3.09 m = 1.4% of the MEASURED 218 m cross-range sigma at 5 km, and displacement equals that sigma only after 35 s (0.028 FPS); a vessel crossing at 5 km moves 0.69 px/s on a 66 deg lens over 640 px. But the INDOOR case is the opposite and is the binding one: at scale 20 a 1.2 m table across 640 px is 1.88 mm/px, so a hand moving a model at 20 cm/s is 106.7 px/frame at 1 FPS, past pi_sensor's 60 px association limit — the track BREAKS, track_length_frames resets to 1, and lane C's persistence precondition makes that vessel INELIGIBLE FOR DARK. Run-sheet rule: move the models at <=5 cm/s. | BLOCKED: NOTHING RAN ON THE PI — the agent cannot reach it (Mac shell is the network-less Linux VM, cloud container is a different machine), so rsync, FPS, CPU temperature and detection count are all Amol's to run. 00_brief/HARDWARE.md §§1-4 still UNMEASURED from session 1. camera_pose_TABLETOP.json still has hfov_deg 0.0 and sensor_node.py refuses to start on it BY DESIGN — the survey now blocks BOTH nodes. ultralytics pulls torch, ~200 MB on aarch64: install before the venue, not on hackathon wifi. lane_f_edge_check.py has never run on the Mac. Lane F still absent from FILE_OWNERSHIP.md.
2026-08-29 | E | OPERATOR CONSOLE DELIVERED AND ACTUALLY RENDERED — plus a prioritizer defect found by rendering it. 03_src/web/index.html (one file, ~69 KB, vanilla JS, NO build step, NO bundler, Leaflet the only frontend dependency), 99_scratch/lane_e_contrast.py, 99_scratch/lane_e_console_render.png. Static checks now 16/16. UNLIKE server.py, THIS ONE WAS EXECUTED: node --check on all five script blocks AND on them concatenated (separate <script> tags share one global scope, so a duplicate top-level const across blocks is a real failure the per-block check misses); then rendered in headless Chromium at 1920x1080 against a stub serving scene01/ranked.json — 0 page errors, all 8 records rendering ranks 1-8 across all four verdict labels, document.scrollHeight <= innerHeight so NOTHING IS BELOW THE FOLD, panes measuring 427/427/589/589 px. THE IDENTITY GUARD WAS VERIFIED FIRING, not merely written: a second stub served a real-looking MMSI (219000606) and rank 1 rendered REDACTED with the alarm banner up. Map geometry verified via a Leaflet TEST DOUBLE recording the calls (1 FOV wedge, 1 cable polyline, 3 infrastructure markers, 1 asset, 27 uncertainty quads and claim markers with correct tooltips) — the double tests MY code, which is the unverified part, rather than Leaflet, which is known good. NOT VERIFIED: Leaflet's actual rendering (neither the CDN nor the npm registry was reachable from the container; the map pane correctly showed its honest fallback message instead) — CHECK THIS FIRST ON THE MAC. DEFECT FOUND BY RENDERING THE BREAKDOWN AND CHECKING THE SUM: 6 of 8 records in ranked.json violate breakdown_sums_to_score(tol=5e-4), with an exact signature — score / sum(component_scores*component_weights) = 0.6500 for every record where defer_to_human is true and 1.0000 for both where it is false. 0.65 is DEFER_EVIDENCE_DAMPING, whose own comment says "Applied before the weighted sum so the published breakdown still adds up". It is not; it is applied to the total, and breakdown_sums_to_score() was evidently never run against a deferred verdict — which is most of any real scene. THE RANKING IS UNAFFECTED (every deferred verdict is damped identically so the order holds); what is wrong is the published EXPLANATION, which for criterion 2 is the deliverable — a judge adding up the column on the slide gets 0.657 where the tool says 0.427. CAVEAT: ranked.json is STALE (old weights 0.32/0.22 not the current 0.30/0.26), so re-run before fixing; the 0.6500 ratio is the signature to look for. Filed to D. The console prints a "BREAKDOWN DOES NOT SUM" warning rather than a total it has not verified, so the fix removes the warning on its own. COLOURS ARE MEASURED, NOT CHOSEN: lane_e_contrast.py computes WCAG ratios for all 17 :root colours against all three grounds plus the inverted solid badges — 18/18 pass, and static check 16 asserts no colour can be added to :root without going through that gate. TWO WERE CHANGED BECAUSE THEY FAILED, and the SPOOF red is the instructive one: #ff7a7a passed on the panel at 7.26:1 and FAILED on a SELECTED row at 6.50:1 — and a SPOOF row is exactly the row that gets selected, so the failure would have landed on the highest-stakes text on the screen. Held to the worst of the three grounds, not the best. Border #2b3644 measured 1.50:1 against a 3:1 gate for meaningful non-text and went to #586b82. HONEST-DESIGN DECISIONS: the observation is drawn as an uncertainty QUAD not a dot, because lane C measured cross-range sigma 218 m against along-range 1250 m at 5 km — a dot asserts precision this sensor does not have AND hides why an along-line-of-sight position spoof is undetectable, whereas the quad is visibly stretched in exactly the direction the deception hides in; the FOV wedge is drawn because a claim with no observation INSIDE it is a finding while the same claim OUTSIDE it is a vessel the camera cannot see; a categorical difference that raised NO mismatch is annotated as such, because claimed "fishing" beside observed "tug" with no note reads as the tool having missed it; "BELIEF IS STALE" turns the node badge amber when online is true but last_seen is older than the threshold, and THAT THRESHOLD COMES FROM /health rather than a number invented in the page. TWO LAYOUT DEFECTS WERE FOUND BY LOOKING AT A SCREENSHOT AND WOULD NOT HAVE BEEN FOUND OTHERWISE — the DOM dump reported every element present and correct, and they were: detection boxes drawn on top of the no-video message, and the operator action buttons sitting ~200 px BELOW THE FOLD. Fixed with a top-pinned message and a sticky action bar; the buttons are now always reachable, which for a decision-support tool is not cosmetic. SERVER.PY CHANGED IN FOUR WAYS, one of them a real hole: (1) EVERYTHING OUTBOUND IS NOW PSEUDONYMISED — run_scene() returns REAL claimed_mmsi/name/imo/callsign and both /api/state and the WebSocket served them verbatim; harmless on the synthetic scene BECAUSE those identities are already 999-prefixed, which is precisely what made it dangerous, since it stays invisible until someone points the server at the real Fehmarn golden window and a real named vessel appears on a projector under a SPOOF badge. Two record stores now: _records (real, reachable only through /evidence behind its two gates) and _public_records (pseudonymised, everything the console sees), scrubbed through evidence.record_to_dict() so assert_no_real_identities() runs on every outbound record, with ONE shared Anonymiser per run — a fresh instance per record allocates index 0 every time and turns EVERY vessel into 999000001, i.e. sixteen hulls merged into one while looking correctly anonymised. (2) /assets static mount for a local Leaflet copy. (3) --web defaults to 03_src/web. (4) /health publishes node_stale_after_s. 04_demo/web/index.html WAS NOT TOUCHED, deliberately and for an operational reason: app.py has no WebSocket, so replacing that file would have deleted the one path that has actually been run end to end in order to add a feature. Lane E's page also polls /api/state when the socket is down, so it runs against app.py too and says on screen which server it is talking to. | BLOCKED / OPEN: (1) Leaflet rendering UNVERIFIED — vendor it on hotel wifi (curl commands in handoff_E.md §10) and open the page once before the venue. (2) server.py still NEVER EXECUTED. (3) The prioritizer damping defect, filed to D — re-run the pipeline first, ranked.json is stale. (4) FILE_OWNERSHIP.md lane E row now also needs 03_src/web/ and the two new scratch files.
2026-08-29 | F | SOURCE FAILOVER BUILT AND MEASURED, AND A P0 FOUND THAT KILLS THE WHOLE DEMO. **THE P0 FIRST: 03_src/verdict.py:122 imports ConsistencyResult and independent_dimension_count from consistency.py and NEITHER EXISTS.** Measured by import: verdict, run_pipeline, server AND app all raise ImportError — BOTH consoles are dead, including the stdlib fallback that was supposed to be the safety net, because it imports run_pipeline too. mtimes show consistency.py 06:11 and verdict.py 06:37, i.e. this broke AFTER the 05:30 clean end-to-end run. Patching the import will NOT fix it: check_all() returns dict[str, list[Mismatch]] while decide_all() expects dict[str, ConsistencyResult] and calls _coverage_fraction() and independent_dimension_count() on each value, so a stub moves the failure from startup into the middle of the demo, which is strictly worse. Filed to lane C as a design reconciliation, not a typo; lane F did not touch either file. NOTHING BELOW HAS RUN END TO END BECAUSE OF IT. FAILOVER, BUILT: 03_src/source_switch.py (237, new, lane F) + nine additive edits to server.py (backup .bak-lanef, disclosed in requests.md) + a SOURCE badge and three switch buttons in index.html (backup .bak-lanef) + 04_demo/FAILOVER.md (146) + 99_scratch/lane_f_failover_check.py. THE DESIGN, IN ONE LINE: the switch selects an AUTHORITY, it does not move data. SourceSwitch holds NO reference to the contact store, record store, evidence queue or anonymiser, so it CANNOT clear them — a property of the object graph, asserted by inspecting __dict__, not a promise kept by careful coding. A switch written as 'reset then re-seed' passes every test on a good day and loses the operator's queue on the one day it fires. WHAT MAKES 5 SECONDS ACHIEVABLE: every ingest buffers contacts BY SENSOR KIND even when that node is not the authority, so switching promotes contacts already in memory; run sheet rule is RUN BOTH NODES FROM THE START. A demoted node is still accepted for LIVENESS but not promoted, so a rebooting Pi cannot silently seize the picture back and the console can say 'posting but not live', which is a different sentence from 'offline'. THE SWITCH IS DELIBERATELY NOT GATED ON THE NODE BEING DECLARED DEAD — detection is for the strip, the switch is for the operator, and they run in parallel. NODE_STALE_AFTER_S 30.0 -> 8.0, derived: contacts every 1.0 s and a heartbeat every 2.0 s, so 8 s is four missed heartbeats; at 30 s an unplugged Pi stayed green for half the pitch. THE HEARTBEAT IS WHY ANY OF IT WORKS: sensor_node.py previously posted ONLY when it had contacts, so a healthy node watching an empty sea posted nothing and the watchdog declared it dead — the exact quiet-queue-vs-dead-sensor ambiguity SensorNode exists to resolve, reintroduced from the sensor end. Heartbeats carry heartbeat:true so the server records liveness without blanking the picture or spinning the pipeline. sensor_node.py also gained open_capture() with a cv2 backend so the SAME node runs on the MacBook as MAC_CAMERA — one pipeline, one bearing model, only the frame source swaps. MEASURED (cloud container, n=3000): SourceSwitch.switch() p50 0.0032 ms, p95 0.0072 ms, worst 0.1237 ms; 24 Part-A checks all pass. Worst case for the console to show OFFLINE = 8 s threshold + 1 s tick = 9 s. | NOT MEASURED: recompute_ms, the half that is the actual cost — Part B is SKIPPED in the container because pyproj is absent and a recompute timed against a substitute geodesy library is not a measurement of this system. Also unmeasured: the physical unplug, which adds the node's own detection latency on top. Both need the Mac, and both need the lane C P0 fixed first. Everything in 00_brief/HARDWARE.md and 04_demo/edge_benchmark.md is still UNMEASURED; camera_pose_TABLETOP.json still has hfov_deg 0.0 and BOTH nodes refuse to start on it by design.
2026-08-29 | E | DEMO HARDENING + REHEARSAL, MEASURED AGAINST A REAL WEBSOCKET. Three failure modes closed and then tested by actually killing the server. Delivered: /demo/inject in 03_src/server.py, reconnect+refresh+injection hardening in 03_src/web/index.html, 99_scratch/lane_e_rehearse_server.py, screenshots lane_e_console_down.png and lane_e_console_injected.png. Static checks 16/16, JS parses concatenated, 0 page errors across every run. HOW IT WAS TESTED: headless Chromium against a hand-written stdlib RFC-6455 stub (this container has no fastapi, no websockets, no wsproto and no route to PyPI, so uvicorn cannot serve a WebSocket at all; the console only ever RECEIVES, so a handshake plus unmasked server-to-client text frames is ~60 lines). The stub's fidelity point is the whole rehearsal: SEQ RESETS TO 0 ON EVERY PROCESS START, exactly as server.py's counter does, which is what makes a restart different from a dropped socket. MEASURED: refresh mid-demo -> full picture back in 239 ms with the selection AND the open evidence panel preserved; kill -> console notices in 5 ms; kill -> honest SERVER UNREACHABLE state in 2.0 s (one poll interval, it waits to be sure); while down, 8 of 8 rows still on screen, desaturated, every pane heading marked STALE, age counting up and retry counting down; backoff intervals actually observed 0.54/1.06/2.48/3.68/6.37 s against an intended 0.5/1/2/4/8 with +/-25% jitter; RESTART FROM THE BACKOFF CAP (the worst case) -> live again in 0.21/2.01/2.02 s, median 2.01, max 2.02; stream verified genuinely FLOWING after restart (seq 0 -> 3), not merely connected. THE SERVER HALF IS NOT IN THOSE NUMBERS and cannot be measured here — 99_scratch/lane_e_rehearse_server.py runs on the Mac and reports boot_ms (import + scene load) and pipeline_ms (run_scene over the whole scene) SEPARATELY, because they have different causes and only one is worth attacking. Total an operator sees = server restart + <=2.02 s client. TWO DEFECTS THE REHEARSAL FOUND, NEITHER FINDABLE BY READING: (1) A DEAD SERVER REPORTED A HEALTHY FALLBACK. Every reconnect attempt ends in onclose, which called setTransport("poll") unconditionally, so with the server killed the header read "polling every 2 s — the picture below is live but slower" while the polls were ALSO failing. The page was describing a fallback that was not running — precisely the looks-fine-but-isn't failure this work exists to prevent, and it survived my own review because the code is correct in every state except the one you cannot reach by reading. Poll health is now tracked separately from socket health and "poll" is only displayed after a fetch has actually succeeded; the down state now says "neither the socket nor the REST fallback is answering. THE PICTURE BELOW IS HISTORY." (2) RECOVERY TOOK 7.09 s WHEN THE RESTART LANDED BADLY (1.48 s on the first run, 7.09 s on the second — the difference is where in the backoff the restart fell). On stage the number that matters is the worst case. The fix is that the 2 s poll was ALREADY a liveness probe and was not being used as one: a successful /api/state fetch is proof the server is back, so the pending retry is cancelled and the socket reopened at once. Recovery is now bounded by the poll interval — 2.02 s worst case REGARDLESS of how far the backoff had escalated. THE RESTART BUG THAT WOULD HAVE BEEN WORST: seq is monotonic PER CONNECTION and restarts at 0 with the process, so a client keeping its old lastSeq across a restart discards every event from the new server as "already folded in" — it would reconnect, LOOK CONNECTED, and never update again, with nothing on screen saying so. The snapshot's last_seq is therefore ASSIGNED, never max()'d, and a lower last_seq raises an 8 s self-clearing notice. The claim is deliberately narrow because the rehearsal showed it is: it fires when the CLIENT holds a higher seq than the server now has — the case that would stall the stream — and a server that restarts having emitted nothing is not announced, correctly, because nothing was lost. REFRESH SAFETY: the DATA half was already solved by ConsoleState; what the server cannot restore is WHICH CONTACT the operator was reading, and hunting for the row while a judge waits is the real cost. The selected record_id goes in sessionStorage (survives a refresh, does not survive closing the tab, so tomorrow's demo does not open on yesterday's selection), wrapped in try/catch because a browser with site data blocked throws on ACCESS and a console that will not start because it cannot remember a selection has traded a small problem for a total one. If the remembered contact is gone the page SAYS SO rather than silently selecting another — a quiet substitution is how an operator stops trusting a tool. SCRIPTED INJECTION: POST /demo/inject with scenarios spoof / dark / reset / prepared-contacts, GET to read the current state. THE LABEL IS INSIDE THE CONTRACT OBJECT, NOT BESIDE IT — an injected contact gets contact_id "inj-…" and frame_ref "injected://spoof/…", and frame_ref is a real EoContact field that evidence.build_record() copies into EvidenceRecord.frame_refs, so THE WORD INJECTED SURVIVES INTO THE EXPORTED CASE FILE at /evidence/{id}. A judge downloading the evidence sees it without being told, and no code path can produce an injected contact whose provenance is absent; a UI-only badge would be a promise, this is a property. Shown in four places and verified in the browser: header chip, dashed queue badge, evidence-header badge, and THE FIRST LINE OF THE LIMITATIONS above the real ones, because a scripted contact that reads like a real detection is the most misleading thing this console could show. The detection box is drawn DASHED so the distinction survives a black-and-white projector. THE INJECTION DOES NOT TOUCH bearing, range, their uncertainties or the camera pose: association is geometry-only, so pristine geometry is what guarantees the injected contact pairs with the SAME AIS claim the original did, and only the attributes under comparison change — which is what a spoof IS, a claim and an observation that disagree about what the hull is while agreeing about where it is. Injection PINS THE SOURCE TO RECORDED (a scripted moment a live node can overwrite two seconds later is not scripted), NAMES AN OPERATOR as /action does, and is DELIBERATELY NOT written to operator_audit.jsonl — that file records decisions about vessels and mixing demo stagecraft into it would corrupt the one artefact whose value is that everything in it is a human judgement about a contact. Default target is the matched contact with the WORST current rank so the re-rank is visible; the chosen id is returned so a rehearsed demo can pin it. | BLOCKED / OPEN: (1) server.py STILL NEVER EXECUTED — /demo/inject, the injection scrub path and every route remain unrun; the container cannot install fastapi/pyproj/loguru. (2) Server-side restart time UNMEASURED — run lane_e_rehearse_server.py. (3) Leaflet rendering still unverified; vendor it on hotel wifi. (4) The prioritizer damping defect (6/8 records, ratio exactly 0.6500 = DEFER_EVIDENCE_DAMPING) still open with D — re-run the pipeline first, ranked.json is stale. (5) FILE_OWNERSHIP.md lane E row now also needs 03_src/web/, lane_e_rehearse_server.py, lane_e_contrast.py and the three PNGs.
2026-08-29 | ARCH | ONE COMMAND, AND THE P0 THAT BLOCKED IT. `python 03_src/main.py` now starts preflight + AIS replay + edge ingest + fusion + server + browser in one process. FIRST, THE BLOCKER, because nothing below could run past it: verdict.py:122 imported ConsistencyResult and independent_dimension_count from consistency.py and NEITHER EXISTED — run_pipeline, server AND app all raised ImportError, i.e. every entry point including the rehearsed stdlib fallback. NOT FIXED WITH A SHIM, deliberately: verdict._coverage_fraction is (agreements+mismatches)/(+uncomparable), so a stub with empty lists gives every HONEST vessel coverage 0.00, drops it under MATCH_COVERAGE_FLOOR and returns UNKNOWN+defer — the shim compiles and destroys the output. consistency.py now returns the real THREE-WAY outcome (Mismatch | Agreement | Uncomparable); each check's `return None` was classified into agreed-vs-could-not-compare at the point of decision, so there is one source of truth and zero `return None` left in the file. 18 anchored edits. | SECOND, THE HARD RULE THAT WAS BEING VIOLATED: an LLM COULD REACH A VERDICT, live, not latent. eo_vlm caps VLM confidence at 0.95, which under the OLD normal quantile was 1.645 sigma — below lane C's 2.0 floor — so an uncalibrated model was structurally inert. Lane C swapped _probability_to_sigma to LOG-ODDS on 2026-08-29 and ln(0.95/0.05)=2.944 is ABOVE 2.0. The guarantee died in another lane's file and nothing caught it because lane B and lane C deliberately do not import each other. MEASURED: a 3.0-sigma length finding alone = MATCH at 0.308; the same finding plus ONE model class token = SPOOF at 0.911, past the 0.85 stating threshold. FIXED by a DERIVED constant, not a chosen one — Tolerances.class_min_report_sigma = 3.5 nats, which restores BOTH broken guarantees at once: the 0.95 ceiling (2.944) is inert again AND a confusable pair (max ln(0.999/0.001)*0.5 = 3.453) can NEVER fire, ever. Also fixed the unit error underneath it: verdict._p_given_honest inverted a NORMAL tail on a LOG-ODDS value, giving the class dimension a likelihood ratio of 185 with no probabilistic meaning at all; it is now inverted on its own scale, 1/(1+e^s). | THIRD, A VIOLATION WITH THE OPPOSITE SIGN, found by running the fix: a VLM confidence of 0.05 dragged _observation_confidence from 0.935 to 0.561, under verdict's 0.60 admission floor, SILENTLY DELETING an independent geometric length measurement from the posterior. A model could not raise a verdict but it could erase the evidence for one. Class factor clamped at 0.9. And found by the same run: a below-threshold class DISAGREEMENT was being recorded as an AGREEMENT, which counts toward coverage — so a model saying 'tug' over a claimed 'fishing' vessel lifted coverage 2/5 -> 3/5 and flipped UNKNOWN to MATCH. A model output changed Verdict.label, in the reassuring direction, on evidence pointing the other way. Only claimed==observed is agreement now. | FOURTH, THE REST OF THE CHAIN: run_pipeline.py:111 called decide_all FOUR ways wrong (4 positional args into 2 params, now= instead of decided_at_utc=, and it fed list[tuple[Verdict,Explanation]] into rank_all which does v.association_id) — that call had never executed. verdict.limitation_strings() was DEAD CODE, so every EvidenceRecord shipped the no-calibration-basis declaration while criterion 4's honesty channel sat unused; now wired, records carry 6 limitations. pi_sensor.to_contact() omitted detection_confidence -> HTTP 400 on EVERY post from the classical node, so the fallback for 'COCO boat does not fire on paper' did not exist; fixed with a saturating function of blob area and persistence, floored 0.30 capped 0.90, NEVER 1.0 (a constant maximum inflates DARK confidence with a number nobody measured). pi_sensor's --scale was accepted, documented and NEVER READ — the tabletop rig emitted TABLE-metres labelled as sea-metres, so a 0.4 m model at --scale 20 reported an 0.4 m vessel against a claimed 180 m hull: a colossal, confident, meaningless spoof, dd/mm/yyyy class. Now applied to range and length, NOT to bearing (an angle is dimensionless), which keeps every sigma ratio invariant under scale — that invariance is what makes a tabletop rehearsal predict the real run. edge_client POLL_STALE_AFTER_S 30 -> 8: the 'pinning' to server.NODE_STALE_AFTER_S was PROSE, lane F moved one side and not the other, and a Pi silent for 10 s was OFFLINE to the watchdog and ONLINE to the poller for 22 s. app.py served REAL claimed_mmsi/name/imo/callsign on /api/state while server.py pseudonymised — the REHEARSED FALLBACK was the one that leaked, invisible on the synthetic scene because those identities are already 999-prefixed. | FIFTH, TWO LIVE BUGS IN server.py FOUND BY AUDIT: /evidence allocated a FRESH Anonymiser per request, so every case file came back as 999000001 while the console showed a different synthetic id for the same hull; and a DUPLICATE "source" key in /health meant the console read source.video as undefined -> 'unknown' -> took the video-IS-configured branch and rendered 'VIDEO SOURCE STOPPED' on the DEFAULT T1 invocation with nothing actually wrong. | MAIN.PY OWNS NO PIPELINE LOGIC. It starts things and reports what it started. It defaults --host to 0.0.0.0 (server.py defaults to loopback, which the Pi cannot reach from any network and which fails at the Pi as a timeout indistinguishable from a cable fault) and PRINTS the LAN address to post to. Its preflight checks every import that can stop the chain, not just fastapi/uvicorn — loguru, numpy, scipy, pyproj all import at module top level on the startup path, so a missing loguru raises BEFORE server.py's own guard can name the fallback. It also checks the seam above, scene freshness (detection_confidence present), the pose hfov, and the port, each with the command that fixes it. THE ONE NEW CAPABILITY IS THE REPLAY CLOCK: load_scene reads ais_tracks.jsonl ONCE, so the AIS half of the picture was frozen — contacts could arrive over time but CLAIMS COULD NOT, and everything that exists only in time (a report going stale, a vessel falling silent, a track loitering) was unreachable. Criterion 2 ranks BEHAVIOUR and there is no behaviour without a clock. Each tick takes the latest report per MMSI within --ais-horizon, so a vessel that stops transmitting DROPS OUT and a hull still in frame becomes a contact with no claim — the DARK path arrived at by the same route a real one would take, not injected. Reads through lane A's DmaCsvSource, never a second CSV reader, so the dd/mm/yyyy swap, the 63.5% duplicate receptions, the AtoN buoys and the 'Unknown' strings are all still handled. Two ADDITIVE hooks on server.py (backup .bak-main, disclosed in requests.md): ConsoleBackend.replace_tracks() and create_app(startup_tasks=...). | MEASURED, executed in the cloud container against the patched files (python 3.11.15, pydantic 2.13.3): 99_scratch/lane_c_seam_check.py 22/22 — honest vessel coverage 1.00 -> MATCH 0.950; the criterion 3 exemplar (40 m fishing claimed vs 248 m tanker observed) -> length 6.93 critical + class 4.60 major, 2 independent dimensions, SPOOF 0.999 subtype identity; an uncalibrated model at the 0.95 ceiling moves the label by NOTHING and the confidence by 0.0000; prioritizer breakdown sums to score; pi_sensor payload validates. Replay clock 13/13 including the aged-out-vessel case and the loop rewind. | NOT RUN: the full stack on the Mac. device_bash is the network-less Linux VM and cannot execute .venv/bin/python; the container has no pyproj/loguru/fastapi and no PyPI. association.py and geometry.py are UNCHANGED by this session but have not been re-run since the fix. Amol runs `python 03_src/main.py` — expect the preflight table first; it fails loudly and starts nothing if a dependency is missing.
2026-08-29 | ARCH | INSTRUMENT BUILT, RIG NOT RUN — AND THE REASON IS A HARD BLOCK, NOT A CHOICE. Asked for detection rate per scenario, false positives, Pi FPS, capture-to-visible-rerank latency and the F3 switch time. NONE of the five can be measured from the agent's position and none was invented. THREE WALLS, each independently sufficient: the Mac .venv is unreachable (device_bash is the network-less Linux VM — no python3.12, no pydantic, PyPI answers 403 through the proxy); the cloud container has no pyproj/loguru/fastapi and no package index, so association.py and geometry.py cannot import and the pipeline cannot run there; the Pi is on neither path. Computer use is OFF, so Terminal could not be driven either. Stubbing pyproj was rejected — a detection rate computed against a substitute geodesy library is a measurement of that library, not of this system. | TWO CORRECTIONS TO THE BRIEF: 03_src/report.py was 0 BYTES and has never contained anything, so it could not be 're-run'; and failure_modes.md did not exist anywhere in the repo. Both were written this session, not refreshed. | WHAT WAS BUILT: 04_demo/benchmark.py (the instrument, 4 subcommands + `all`), 04_demo/benchmark.md (method and argument now, every table marked UNMEASURED — the same house style as edge_benchmark.md), 03_src/report.py (1355 lines, generates 04_demo/failure_modes.md from real pipeline output: defer reasons by count, uncomparable dimensions, BOTH false-positive definitions, misses by kind, prior-sensitivity via verdict.is_prior_sensitive, and the structural limits quoted from the files that own them; refuses to write if assert_no_real_identities fails), and a paint-timing hook appended to 03_src/web/index.html. | THE HEADLINE NUMBER HAS A TRAP IN IT AND THE DESIGN IS THE DELIVERABLE. The obvious implementation of capture-to-visible-rerank is browser_paint_time minus EoContact.frame_time_utc, and it is WRONG: those stamps are on two machines, the Pi's clock and the Mac's, and the NTP skew at a venue can exceed the quantity being measured — the result would be confident, precise, meaningless, and can come out NEGATIVE. So the instrument never subtracts two clocks. (A) THE HEADLINE runs the sensor on the Mac so capture, server and browser share ONE clock and t_painted - frame_time_utc is a true end-to-end interval with no skew term and nothing estimated — that is the number to say out loud, with the sentence 'measured on a single clock'. (B) THE PI is decomposed into four legs each timed on one clock, with the Pi->Mac hop as HALF THE POST ROUND TRIP measured entirely on the Pi — the only estimate in the document, labelled as one everywhere it appears. | THE PAINT LEG USES TWO requestAnimationFrames, not one: a single rAF callback runs BEFORE the frame carrying the DOM writes is composited, so it means 'the browser agreed to paint', not 'the pixels are on the glass'. The claim is a VISIBLE re-rank, so the measurement must be visible. Costs ~16 ms at 60 Hz and errs SLOW, which is the correct direction for a number said out loud. | THE COLLECTOR IS DELIBERATELY NOT A ROUTE ON server.py: the shore station is the system under measurement, and putting the instrument inside it makes the measured and measuring process the same process. benchmark.py runs its own listener on :8899, so server.py is byte-identical whether or not anyone is benchmarking and there is nothing to remember to remove before going on stage. The console hook is a SEPARATE <script> block, appended last, STRICTLY INERT without ?bench=1, and wraps applyEvent rather than replacing it — so the demo build and the benchmark build are one file. | SCENARIOS REUSE THE EXISTING HARNESS, nothing reimplemented: make_synthetic_eo.build_scene + run_pipeline.score_scene, with a FRESH SEED PER TRIAL because one fixed scene measures whether the pipeline handles ONE arrangement of vessels and that number cannot generalise but will be quoted as though it can. Identity is split into length vs class deliberately — length is physical extent and cannot be argued with, class rides on a classifier and is capped so it can never convict alone; averaging them hides that the strong half carries the weak one. Wilson intervals, not normal: at 12/12 the normal approximation returns [1.00,1.00], asserting certainty from twelve trials, where Wilson returns [0.76,1.00]. Nearest-rank percentiles, never interpolated — p95 must be a value the system actually produced. Controls ride along in EVERY trial; a scene with no controls cannot measure a false-positive rate and a detection rate without one is a marketing number. | MEASURED (the instrument, not the rig — cloud container, python 3.11.15, pydantic 2.13.3): 18/18 unit checks. The load-bearing ones: an EMPTY run renders UNMEASURED with ZERO digit-bearing ms or % cells, so the renderer structurally cannot fabricate; a PARTIAL run reports the leg that was measured, leaves end-to-end UNMEASURED beside it and emits an explicit do-not-quote warning instead of a pitch line; a null measured_fps renders UNMEASURED rather than a plausible 30; the probe contact validates as a real EoContact and carries NO identity field. Console integrity re-verified after the hook: all 6 script blocks pass node --check individually AND concatenated (separate blocks share one global scope — the check the per-block one misses). Backup at 03_src/web/index.html.bak-bench. | NOT RUN, AND THIS IS THE WHOLE POINT: benchmark.py has never met the system it measures. Expect first-run failures. AMOL RUNS: terminal 1 `python 03_src/main.py --camera 0`; terminal 2 `python 04_demo/benchmark.py all --expect-browser --emit 04_demo/benchmark.md`, then open the console at /?bench=1&collector=http://127.0.0.1:8899/paint when it asks. Then `python 03_src/report.py --scene 04_demo/out/scene01` for failure_modes.md.
2026-08-29 | ARCH | FIRST REAL RUN OF report.py ON THE MAC — the pipeline WORKS, and the output is not flattering. failure_modes.md generated (23 KB, 8 verdicts over scene01). PREFLIGHT FAIL DIAGNOSED: it is camera_pose_TABLETOP.json (hfov_deg 0.0, height_m 0.0), which is the long-open SURVEY task, not a code defect — both sensor nodes refuse a zero HFOV by design because a zero HFOV makes every bearing meaningless silently rather than loudly. The check only runs when --camera is passed, so `python 03_src/main.py` with no camera starts today; scene, AIS source, seam and port all PASS. | WHAT THE RUN FOUND, all measured: detection 2/6 = 33.3%; 8 of 8 verdicts deferred (100%), every one carrying low_confidence; class coverage 0% (4 of 4 matched pairs uncomparable — 2 below the 0.60 classifier floor, 2 below the 3.5 nat class floor); overall coverage 80%. THE WORST ROW: an injected `dark` vessel came back SPOOF/identity at confidence 1.00 — not a miss but a WRONG ACCUSATION, the failure mode that matters in court, and the orphan-corroboration rule exists precisely to prevent it. TWO MISSES CAME BACK MATCH (length_spoof 5.0x, 26m->130m, conf 0.76; class_spoof tanker->passenger, conf 0.76) — the tool vouching for a claim that was false. | ROOT CAUSE OF THE 100% DEFERRAL FOUND AND FIXED, and the generated document diagnosed it itself in §6.2: run_scene() never passed an ais_coverage_confidence, so verdict.py fell back to DEFAULT_AIS_COVERAGE_CONFIDENCE = 0.70, which is BELOW SPARSE_COVERAGE_THRESHOLD = 0.75 — so EVERY dark verdict deferred on coverage grounds in every scene, forever, as a property of an unstated default rather than of the data. That is a criterion-2 problem, not a cosmetic one: a ranked queue in which every entry is deferred recommends nothing and the prioritiser cannot separate anything by a flag that is constant. FIXED by making it an explicit argument through run_scene -> app.State -> create_app -> main.py --ais-coverage-confidence, with the value AND its basis stamped into every result dict so failure_modes.md prints what was assumed. THE DEFAULT IS DELIBERATELY UNCHANGED — moving a threshold to make a demo look better is the exact move this project forbids; what changed is that the assumption is now visible and settable. MEASURE it for a real sector via ais_trajectory.detect_gaps rather than picking one. Verified 8/8 statically (signatures, call chain, main.py no longer reaches into server internals, default unmoved). Backups .bak-cov. | STILL OPEN: the dark->SPOOF mislabel and the two MATCH-on-a-false-claim misses are UNDIAGNOSED and need a run; they are the pitch risk, not the deferral rate.
